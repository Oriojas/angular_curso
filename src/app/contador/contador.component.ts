import { Component } from "@angular/core";

@Component({
    selector: 'app-contador', // etiqueta para llamar el componente
    templateUrl: './contador.component.html', 
    styleUrls: ['./contador.component.css'] // se puede linkear con mas css
})

export class ContadorComponent {

}